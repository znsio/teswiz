import { unsupportedScreenAction } from "../screen-route.ts";

export function swipeRight(): object {
  return unsupportedScreenAction(
    "JioCinema is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function swipeLeft(): object {
  return unsupportedScreenAction(
    "JioCinema is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function scrollTillTrendingInIndiaSection(): object {
  return unsupportedScreenAction(
    "JioCinema is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function isMovieNumberVisibleOnScreen(): object {
  return unsupportedScreenAction(
    "JioCinema is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function swipeTrendingItem(): object {
  return unsupportedScreenAction(
    "JioCinema is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}
