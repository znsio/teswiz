import { unsupportedScreenAction } from "../screen-route.ts";

export function launchBrowser(): object {
  return unsupportedScreenAction(
    "DuckDuckGo is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function cancelChangingDefaultBrowserPopup(): object {
  return unsupportedScreenAction(
    "DuckDuckGo is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function getDefaultTextFromWebView(): object {
  return unsupportedScreenAction(
    "DuckDuckGo is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function switchToNativeContextAndGoToTeswizGithub(): object {
  return unsupportedScreenAction(
    "DuckDuckGo is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}
