import { unsupportedScreenAction } from "../screen-route.ts";

export function searchByImage(): object {
  return unsupportedScreenAction(
    "Home is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function attachFileToDevice(): object {
  return unsupportedScreenAction(
    "Home is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function goToMenu(): object {
  return unsupportedScreenAction(
    "Home is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function selectProductFromCategory(): object {
  return unsupportedScreenAction(
    "Home is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function searchForTheProduct(): object {
  return unsupportedScreenAction(
    "Home is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function clickOnAllowToSendNotifications(): object {
  return unsupportedScreenAction(
    "Home is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function clickOnAllowLocation(): object {
  return unsupportedScreenAction(
    "Home is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function clickOnAllowLocationWhileUsingApp(): object {
  return unsupportedScreenAction(
    "Home is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function relaunchApplication(): object {
  return unsupportedScreenAction(
    "Home is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}
