import { unsupportedScreenAction } from "../screen-route.ts";

export function numberOfProductFound(): object {
  return unsupportedScreenAction(
    "Search results is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function selectProduct(): object {
  return unsupportedScreenAction(
    "Search results is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function isProductListLoaded(): object {
  return unsupportedScreenAction(
    "Search results is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function getProductListingPageHeader(): object {
  return unsupportedScreenAction(
    "Search results is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}

export function selectFirstItemFromList(): object {
  return unsupportedScreenAction(
    "Search results is not supported on web for WEB_ENGINE=playwright-ts.",
  );
}
